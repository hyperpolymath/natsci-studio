# This workflow is managed by gh actions-lock.
# SPDX-License-Identifier: MPL-2.0
name: OSSF Scorecard

on:
  pull_request:
    branches: ["**"]
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write
  id-token: write
jobs:
  scorecard:
    uses: hyperpolymath/standards/.github/workflows/scorecard-reusable.yml@e9fa12b965897f485f8ec143e048a424b52f689a
    permissions:
      contents: read
      security-events: write
      id-token: write
